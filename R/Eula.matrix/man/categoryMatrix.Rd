% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudobulkMatrix.R
\name{categoryMatrix}
\alias{categoryMatrix}
\title{Construct a Category (Grouping) Design Matrix}
\usage{
categoryMatrix(
  labels,
  method = c("aggregate", "average"),
  reverse = FALSE,
  drop = TRUE
)
}
\arguments{
\item{labels}{A vector of grouping labels.}

\item{method}{Character string specifying scaling:
\itemize{
\item \code{"aggregate"}: return a binary indicator matrix (0/1)
\item \code{"average"}: scale each column by \code{1 / group_size}
}}

\item{reverse}{Logical. If \code{TRUE}, invert the indicator matrix (1 becomes 0
and 0 becomes 1). When \code{labels} only contain 1 level, \code{reverse = TRUE} is
invalid and will raise an error.}

\item{drop}{Logical. If \code{TRUE}, remove groups with zero samples.}
}
\value{
A numeric matrix (typically sparse) with:
\itemize{
\item Rows corresponding to elements of \code{labels}
\item Columns corresponding to unique label levels
}
}
\description{
Creates a design matrix representing group membership of samples (or cells).
This matrix is typically used for pseudobulk aggregation:
}
\examples{
labels <- c("A", "A", "B", "B", "B")

# Indicator matrix
categoryMatrix(labels, method = "aggregate")

# Averaging matrix
categoryMatrix(labels, method = "average")

}
\seealso{
\code{\link[=pseudobulkMatrix]{pseudobulkMatrix()}}
}
