% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudobulkMatrix.R
\name{pseudobulkMatrix}
\alias{pseudobulkMatrix}
\alias{pseudobulkMatrix.CsparseMatrix}
\title{Construct a Pseudobulk Expression Matrix}
\usage{
pseudobulkMatrix(object, ...)

\method{pseudobulkMatrix}{CsparseMatrix}(
  object,
  cell.groups,
  method = c("aggregate", "average"),
  reverse = FALSE,
  bulk = FALSE,
  sparse = FALSE,
  drop = TRUE,
  ...
)
}
\arguments{
\item{object}{A numeric matrix-like object where rows typically represent
features (e.g. genes) and columns represent cells or samples.}

\item{...}{Arguments passed to other methods.}

\item{cell.groups}{A vector of grouping labels with length equal to
\code{ncol(object)}. Each element specifies the group assignment of a column.}

\item{method}{The method used for calculating pseudobulk expression; one of:
"average" or "aggregate"}

\item{reverse}{Logical. If \code{TRUE}, each column of the output matrix will
represent summaries without corresponding category.}

\item{bulk}{Logical. If \code{TRUE}, an additional \code{"bulk"} column is added
containing sums (\code{aggregate}) or means (\code{average}) across all columns.}

\item{sparse}{Logical. Whether to return a \code{CsparseMatrix} or \code{matrix}.}

\item{drop}{Logical. Whether to drop empty groups (groups with zero columns).}
}
\value{
A dense or sparse matrix with:
\itemize{
\item Rows corresponding to \code{rownames(object)}
\item Columns corresponding to the levels of \code{cell.groups}
\item If \code{bulk = TRUE}, the first column is \code{"bulk"}.
}
}
\description{
An S3 generic function to generates a pseudobulk matrix by aggregating or
averaging columns (e.g. single cells) of a matrix-like object according to
grouping labels.
}
\examples{
set.seed(1)
mat <- matrix(rpois(20, lambda = 5), nrow = 5)
groups <- rep(c("A", "B"), each = 2)

# Aggregate (sum) by group
pseudobulkMatrix(mat, groups, method = "aggregate")

# Average by group
pseudobulkMatrix(mat, groups, method = "average")

# Include bulk column
pseudobulkMatrix(mat, groups, bulk = TRUE)

}
\seealso{
\code{\link[=categoryMatrix]{categoryMatrix()}}
}
