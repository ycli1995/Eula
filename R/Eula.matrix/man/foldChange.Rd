% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/foldChange.R
\name{foldChange}
\alias{foldChange}
\alias{foldChange.CsparseMatrix}
\alias{foldChange.matrix}
\title{Compute Fold Change Between Two Cell Groups}
\usage{
foldChange(object, ...)

\method{foldChange}{CsparseMatrix}(
  object,
  cells.1,
  cells.2,
  mean.fxn = rowExpMean,
  min.exp = 0,
  pseudocount.use = 1,
  base = 2,
  verbose = TRUE,
  ...
)

\method{foldChange}{matrix}(
  object,
  cells.1,
  cells.2,
  mean.fxn,
  min.exp = 0,
  pseudocount.use = 1,
  base = 2,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A numeric matrix-like object where rows represent features
(e.g. genes) and columns represent cells.}

\item{...}{Arguments passed to other methods.}

\item{cells.1}{A vector of column indices or names defining group 1.}

\item{cells.2}{A vector of column indices or names defining group 2.}

\item{mean.fxn}{A function used to calculate the means of features. Default
is \code{\link[=rowExpMean]{rowExpMean()}} in non-logged space.}

\item{min.exp}{Minimum threshold used when computing the percentage of
expressing cells. Default is 0.}

\item{pseudocount.use}{Pseudocount added to group means before computing
fold change. Default is 1.}

\item{base}{Logarithm base used for fold change calculation. Default is 2.
\itemize{
\item If \code{base > 0}, log fold change is computed:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{log(mean.1) - log(mean.2)
}\if{html}{\out{</div>}}
\item If \code{base < 0}, simple mean difference is returned:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mean.1 - mean.2
}\if{html}{\out{</div>}}
}}

\item{verbose}{Logical, whether to print the progress information to console.}
}
\value{
A \code{data.frame} with one row per feature and columns:
\itemize{
\item \verb{avg_log\{base\}FC} (or \code{avg_diff} if \code{base < 0})
\item \code{mean.1}, \code{mean.2}: average expression per group
\item \code{ncells.1}, \code{ncells.2}: number of cells in each group
\item \code{pct.1}, \code{pct.2}: percentage of expressing cells in each group
}
}
\description{
Computes per-feature fold changes between two sets of columns (e.g. two cell
groups) in a matrix-like object.
}
